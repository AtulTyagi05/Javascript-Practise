const obj = {

    team1 : "Sachin",
    team2 : "Saurav",
    team3 : "Rahul",
    team4 : "Shewag",
}

// For in loop

for (const key in obj) {
    // console.log(`${obj[key]} is the best player of ${key}`);
}


obj.forEach(element => {
    
    console.log(element[key]);
    
});